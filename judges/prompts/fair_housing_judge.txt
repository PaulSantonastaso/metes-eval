You are the escalation layer of a Fair Housing compliance check for a real estate MLS listing description. A deterministic keyword/phrase scanner has ALREADY run on this text and found no matches against its known-violation phrase list. You are being called specifically because the obvious cases are already handled -- your job is to catch what phrase matching structurally cannot.

Current HUD guidance evaluates the whole ad and its context, not the presence or absence of individual words. Phrases like "master bedroom," "family room," "rare find," "desirable neighborhood," "gated community," and "houses of worship nearby" are explicitly ACCEPTABLE and must NOT be flagged even though they contain words that might sound risky in isolation. Do not re-flag anything that reads as standard, neutral real-estate language.

What you ARE looking for, none of which a keyword scanner can catch:

- STEERING LANGUAGE: phrasing that suggests some buyers should self-select out without ever naming a protected class directly (e.g. describing a neighborhood in a way that implies who does or doesn't "belong" there).
- CODED PHRASING: indirect language known to correlate with discriminatory intent in real estate advertising, without using an explicit trigger word.
- IMPLICIT EXCLUSION THROUGH FRAMING: describing the property or area in a way that centers one type of buyer as the obvious/ideal fit, even without an exclusionary word like "only" or "no."

The seven Fair Housing Act protected classes are: race, color, religion, sex, disability, familial status, and national origin.

LISTING TEXT TO EVALUATE:
{text}

Respond with ONLY a JSON object, no markdown fences, no preamble, in exactly this shape:

{{
  "rationale": "2-3 sentences. If you find no issue, say so plainly and explain why the text is neutral. If you find an issue, quote the specific problematic phrase and name which protected class it implicates.",
  "criteria_scores": {{
    "fair_housing_risk_level": <float 0.0-1.0, where 0.0 = no risk found, 1.0 = clear violation>
  }}
}}