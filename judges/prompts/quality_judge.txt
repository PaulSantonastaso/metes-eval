You are evaluating an MLS listing description and headline generated by a real estate marketing AI, for QUALITY -- not for hard-fact accuracy. A separate deterministic system already checks whether stated facts (bedrooms, bathrooms, square footage, named features) match the source data. Do NOT re-check those; assume that layer has already run.

Your job covers three things a deterministic checker cannot:

1. UNSTRUCTURED HALLUCINATION: Does the copy invent qualitative claims not supported by the property data or notes? Examples: "recently renovated" when nothing indicates renovation, "quiet cul-de-sac" when the address data doesn't support it, "chef's kitchen" when appliances are standard. Do not flag claims that are reasonable stylistic inference from real features (e.g. calling a screened lanai "perfect for morning coffee" is acceptable framing, not hallucination).

2. COMPLETENESS: Does the copy make good use of the standout features actually provided (key_features, interior_features, exterior_features)? Copy that ignores several provided standout features in favor of generic filler should score lower.

3. TONE: Does the copy read as specific and tailored to this property, or as generic template language that could apply to almost any listing? Specific, sensory, concrete language scores higher than vague superlatives ("stunning," "must-see," "won't last") used without supporting detail.

PROPERTY DATA (source of truth):
{property_context}

GENERATED HEADLINE:
{headline}

GENERATED MLS DESCRIPTION:
{mls_summary}

Respond with ONLY a JSON object, no markdown fences, no preamble, in exactly this shape:

{{
  "rationale": "2-4 sentences explaining your scores, citing specific phrases from the copy",
  "criteria_scores": {{
    "hallucination_free": <float 0.0-1.0, where 1.0 = no unstructured hallucination detected>,
    "completeness": <float 0.0-1.0>,
    "tone": <float 0.0-1.0>
  }}
}}